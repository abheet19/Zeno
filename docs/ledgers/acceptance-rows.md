| SUITE-AC-01 | The requirement ledger and traceability matrix cover 100% of this prompt, including conflicts, corrections, phased/deferred items and acceptance criteria. | Machine-checked zero-unmapped report plus explicit conflict/defer register. |
| SUITE-AC-02 | Assistant/Command, Forge and Counsel each launch, work, save/export and recover independently. Stopping Command or either sibling does not terminate or corrupt another product; reconnecting does not duplicate an action. | Standalone and reconnect demo for all three, including shared-service degradation. |
| SUITE-AC-03 | Shared protocol, orchestrator, policy, approvals, context/Vault, activity stream, model gateway, MCP, connectors/OAuth, device Link and Glass tokens are versioned reusable packages/services, not copied app logic or ambient database access. | Package/dependency map, no-cycle check, contract tests, mock adapters and independent build/release evidence. |
| SUITE-AC-04 | One root-agent conversation can hand off among Mac HUD/menu bar, Command chat, global launcher, voice/PTT/wake, phone/mobile, browser/IDE, CLI/TUI and Forge/Counsel panes with one canonical task/thread identity, permissions, approval/execution stream and exactly one active executor. Every command names or visibly resolves the exact target device and limited interfaces disclose unsupported capabilities. | Cross-interface and cross-device handoff showing preserved transcript, context, plan, artifacts and approvals; stale/revoked/ambiguous device tests; and no lost/duplicate turn or action. |
| SUITE-AC-05 | Mac, phone and web show a reconnect-safe Observable Execution Stream with goal, phase, editable plan, agent/model, target device/repository/account, redacted tool/action, evidence, files/diffs/tests, elapsed time, budget, checkpoint, result/blocker and next approval. | Live and replayed run with ordered events, gap recovery and no hidden reasoning or secrets. |
| SUITE-AC-06 | From every supported activity surface, the user can ask, steer, edit the plan, pause, resume, cancel, stop at the next safe boundary or take over; a global kill switch revokes outstanding capabilities. | Interruption tests during streaming, tools, multi-agent fan-out, device handoff, reconnect and pre-external-effect state. |
| SUITE-AC-07 | MCP is first-class: Forge ships the required host/client and narrow optional server; Command governs registry/grants; Counsel exposes only scoped read-only methods. | Protocol/contract suite covering discovery, stdio/Streamable HTTP, auth expiry, cancel/reconnect, schema change, malicious server output, injection, per-agent isolation and revoke. |
| SUITE-AC-08 | Every OAuth-capable integration shows provider, acting account/workspace, scopes and changes, token owner/expiry, health, reconnect and revoke; OAuth/OIDC/PKCE/state/nonce behavior is correct where applicable and no credential reaches a model or log. | Connect, wrong-account, refresh/expiry, scope-expansion denial, revoke/cascade and secret-scanning tests. |
| SUITE-AC-09 | Every shipped dependency, model, dataset, font, icon, sound, shader, 3D asset and MCP has pinned provenance and compatible licensing, and the product license is explicit. | `LICENSE`, `THIRD_PARTY_NOTICES`, cards, lockfiles, SBOM, provenance report and CI license result. |
| SUITE-AC-10 | When an authorized required web item/export cannot be accessed, Claude asks for the minimum official download with URL, item/version/date range, format/size/checksum where known, purpose, sensitivity, license/cost and import/deletion steps; it never asks for passwords/cookies or bypasses access. | Manual-download request card and resumable quarantined import test. |
| SUITE-AC-11 | Temporary/private sessions can read no durable memory and contribute no future memory; configured audio/transcript/screenshot/chat/artifacts expire as disclosed. | Filesystem/database/vector/cache/backup inspection after expiry, with only the pre-disclosed minimal audit tombstone. |
| SUITE-AC-12 | Offline, connector/model/device failure, low disk, thermal pressure, crash and network partition are visible and recoverable; partial work is never presented as complete. | Fault injection, restart, checkpoint recovery, duplicate suppression and truthful rollback/compensation report. |
| SUITE-AC-13 | A scoped Workspace Context Scope Record captures the declared and approved repositories/sources/purposes/providers/retention/egress and expiry once. Unchanged in-scope local work is not repeatedly blocked, while scope/provider/egress changes require a visible delta review. | Scope-record lifecycle, expiry, scope-change, revocation and cross-zone-denial tests. |
| SUITE-AC-14 | Before Phase 0, Claude proves that this complete master requirements file—not a truncated paste—was loaded by recording its path/source, cryptographic hash, bytes, lines, first/last sentinels and parsed requirement/acceptance counts. If the client cannot retain it intact, it stops and requests the Markdown file as an attachment/context resource. | Hash/size/line/sentinel receipt plus machine-checked zero-unmapped Requirements Traceability Matrix and forced-truncation test. |
| WIN-FIRST-AC-01 | Before any suite binary or privileged runtime trial reaches the work Mac, one Windows release candidate completes independently launched primary journeys for Assistant/Command, Forge and Counsel plus Vault and Link on the declared user-controlled Windows pilot machine. | Signed build/host manifest; synthetic/personal end-to-end journeys with Command stopped where applicable; Windows capability, accessibility and limitations report. |
| WIN-FIRST-AC-02 | Windows validation covers local voice/dictation; exact Windows app/browser/Explorer/terminal control; model/MCP fixtures; draft/approval/outbox; Forge LLD/build/test/browser QA; Counsel capture/suggestions with synthetic or consented inputs; Vault sync/conflicts; phone/web steering; offline/failure/revocation; performance/soak; update/rollback and secure uninstall. | Windows Pilot Gate report with positive/denied/interrupted tests, p50/p95/p99/resource evidence, installation residue scan, unresolved-risk list and Windows→macOS delta matrix. |
| WORK-MAC-GATE-AC-01 | Before I explicitly say “Approve work-Mac pilot,” the work Mac receives no suite binary, launch, pairing/executor grant, permission request, continuous capture, login/background item, OAuth connection, Keychain import, repository/history/Vault ingestion through the suite or privileged OS/browser/terminal action. | Work-Mac negative inventory/audit receipt, device registry, permission/app/login-item/network checks and owner approval event bound to the exact Mac pilot plan/build hash. |
| MAC-NATIVE-AC-01 | Windows evidence is never reused as proof for AXUIElement, Apple Events/App Intents, ScreenCaptureKit/Core Audio, Keychain/Secure Enclave/Touch ID, menu bar, Spaces/Stage Manager, signing/notarization, update/uninstall or any other Mac-native behavior. Each promoted capability starts synthetic/read-only and passes native Mac conformance before work use. | Per-capability Windows→Mac delta, signed Mac build, native positive/denied/interrupted tests, permissions/rollback evidence and no-inherited-pass assertion. |
| LLD-GATE-AC-01 | Every production slice across all products/shared services/platform brokers has an approved cited LLD Artifact and Plan version/hash before its first code, dependency, migration, repository mutation or privileged test; UI-affecting work also has the approved interactive Design Artifact. Gate-level or another platform’s approval cannot substitute. | Chronological repository/dependency/infra proof, artifact/approval ledger mapped to every slice’s first change and changed-scope invalidation tests. |
| XDEVICE-PRODUCT-AC-01 | Assistant, Forge and Counsel each complete an independently launched, capability-appropriate primary journey on every released macOS, Windows, Linux, web, iPhone/iPad and Android client. Every step is truthfully `local`, `remote-delegated`, `review-only`, `takeover-required` or `unavailable`; no client claims host privileges it lacks. | Product × platform × capability matrix; native-platform interactive prototypes; complete journeys with Command stopped; accessibility, offline, degraded and delegated-execution tests. |
| LINK-AC-01 | Every supported controller class can address every compatible authorized executor class through exact device resolution, capability negotiation, signed commands, deterministic policy, visible target indication, immediate local takeover priority and exactly one active executor. Locked/security/biometric/secure-field operations cannot be remotely bypassed. | Controller→executor pairwise matrix covering Mac, Windows/Linux workstation, phone/tablet, web and approved remote runtime; ambiguous target, lock, revoke, compromise, replay, stale lease and executor-race tests. |
| LINK-AC-02 | Cross-device navigation and handoff preserve the canonical session/task, unsent draft, plan, selected artifacts, file/diff or transcript review, current action, approvals and authoritative versions without duplicate execution or cross-zone leakage. Navigation adapts to each platform without changing permission meaning. | Assistant→Forge→Counsel→Vault journeys across at least three devices; deep-link, disconnect, stale route, schema mismatch, concurrent steering, wrong-zone and reconnect tests. |
| LINK-AC-03 | Cross-device route open, action-stream propagation, streaming update, handoff, remote acknowledgement and stop/cancel meet approved p50/p95/p99 LAN/WAN budgets and immediately disclose local versus delegated/offline/degraded/takeover state. Decorative continuity never delays controls. | Monotonic end-to-end traces, packet-loss/high-latency tests, cold/warm clients, foreground/background transitions, reduced-motion/low-power and local-takeover results. |
| LINK-AC-04 | A one-device, named-group or ad hoc multi-target command previews the exact authorized device set, acting accounts, lock/presence/privacy and capabilities before execution; permits deselection; uses per-target idempotency; and reports each target independently. Ambiguous “all devices,” newly paired devices and partial failure can never produce a hidden wildcard or one misleading success state. | Named-group/version-change, newly paired device, exact-preview approve/edit/deny, mixed online/offline/locked/capability, partial failure, cancel, retry, duplicate-delivery and signed per-device receipt tests. |
| CAP-AC-01 | Every connector, MCP, model, device and privileged local adapter registers the complete Capability Broker tuple and one canonical health state; `healthy` never grants authority and stale/unverified capability cannot satisfy a current-state precondition. | Registry/schema conformance; wrong-account, stale watermark, scope change, schema change, quota, reauth, offline, revoked and recovery tests with user-visible evidence. |
| CAP-AC-02 | Every workflow declares dependencies as required, optional or substitutable with freshness/evidence minima and an approved fallback ladder. Every result is truthfully `complete-current`, `complete-with-approved-fallback`, `partial`, `blocked` or `unknown`, with per-source age, coverage and omissions. | Machine-checked workflow manifests plus single- and multi-dependency fault matrix proving no missing service is silently treated as a successful empty result. |
| CAP-AC-03 | Automatic plan repair may choose only an equal-or-lower-risk authorized path in the same identity, scope and data zone. It never silently changes provider/account, broadens OAuth, starts paid/cloud processing, weakens approval or queues a T2/T3 effect for later execution; recovery requires fresh state, preview and approval. | Provider/account/zone/cost mutation denials, reconnect tests, hash-bound approval invalidation and zero delayed consequential effects. |
| CAP-AC-04 | NeoSapien, Jira, Slack, Git provider, Figma, CI, email/calendar, MCP, model, auth, internet, device, storage/index/graph and observability each pass their documented fallback and recovery paths, including at least three concurrent-failure chaos journeys. | Capability/Degradation Matrix, adapter fixtures, deterministic recovery trace, exactly-one-executor evidence and source-coverage reconciliation. |
| PROVIDER-ETHICS-AC-01 | Model/provider fallback uses only authorized owned credentials through the broker and obeys provider quotas, billing, safety controls and terms. The suite contains no key/account/identity cycling, account farming or free-tier/rate-limit evasion; quota exhaustion is a visible degraded/blocked state. | Static/config/secret-flow scan, exhausted-quota and multi-provider failover tests, account/scope audit, cost-cap test and provider-terms review. |
| CAP-CATALOG-AC-01 | Every executable natural-language step resolves to one or more versioned Atomic Action Catalogue entries containing exact target/schema, adapter and platform/app range, pre/postconditions, scopes, effect/risk, permissions, idempotency/reversibility/destructive/secret flags, preview/approval, fallback, recovery, limitations and linked tests. Unknown actions never inherit wildcard shell, vision or computer-use authority. | Machine-readable catalogue/schema validation, intent-to-capability traces, zero-unclassified shipped-action report and unsupported/takeover tests. |
| MAC-COVERAGE-AC-01 | Within the versioned installed-surface plus named mission/command corpus, the Mac Surface Matrix classifies applications/windows/tabs, Finder/files, browser/transfers, text/clipboard, capture/print/scan, notifications/productivity, system/peripherals, application lifecycle, continuity, automation, accessibility, recovery and developer surfaces on separate release-support, runtime-health and conformance axes. A new/out-of-universe request remains unclassified/unsupported/takeover and a newly discovered app remains open-and-focus only until deeper conformance passes. | Finite-universe manifest, per-device/app coverage report, catalogue-to-runtime/UI/voice conformance, version-drift/health-change tests and no-unsupported-“everything” claim audit. |
| MEMORY-AC-01 | Assistant, Forge and Counsel remain stateful across restart, compaction, model switch and device handoff by reconstructing a scoped ContextView from the State Authority Matrix; no model context window, provider history or fine-tuned weight is authoritative task state. | Cross-product restart/model-switch/handoff tests showing goal, plan, corrections, evidence, ACL, approvals and checkpoint continuity without cross-scope leakage. |
| MEMORY-AC-02 | Durable memory and learned personal patterns use the governed proposal lifecycle and explicit scopes. Only explicit statements, accepted corrections, approved outcomes and reviewed repeated evidence may be promoted; one accidental action, rejected output or raw meeting transcript cannot become a permanent preference. | Proposal/accept/reject/expire/supersede/revoke/rollback tests, `this turn/session/project/always` UI, counterexample handling and security-boundary non-learning tests. |
| MEMORY-AC-03 | Cross-product state uses causal parents, aggregate versions, idempotency, leases/epochs and fencing. Offline replicas append proposals only; reconnect detects gaps and restores exactly one executor without duplicating work or overwriting newer authority. | Partition, clock-skew, out-of-order, stale-writer, duplicate-event, crash and tombstone-replay tests with event/checkpoint reconciliation. |
| MEMORY-AC-04 | The Second-Brain experience answers and suggests with citations, as-of time, coverage, unavailable sources, conflicts and fact/snapshot/memory/inference labels; source revocation/deletion cascades through Vault, indexes, graph projections, Obsidian, caches and paired devices. | Cited recall/proactive-attention evaluation, inaccessible-source test, graph/list parity, source-age assertions and deletion-cascade receipts. |
| SAN-AC-01 | Every ingress, prompt/model, tool/MCP, UI/notification, log/telemetry, memory/Vault/vector/graph, cache/storage/backup, dev-preview and export path is mediated by a registered Context Sanitization Gateway sink contract; no direct raw-to-consumer path exists. | Machine-checked data-flow/sink registry, architecture dependency test, filesystem/network instrumentation and deliberate bypass denials. |
| SAN-AC-02 | Raw Evidence/Quarantine and purpose-bound sanitized views use separate stores, keys and ACLs. Every view binds exact source revision, purpose, task, consumer/provider/region, sink, zone/consent, policy/detector version and expiry; sanitization never broadens authorization. | Cross-zone/provider/account tests, storage inspection, wrong-view replay denial and opaque-ID/keyed-digest leakage scan. |
| SAN-AC-03 | Models receive typed placeholders rather than credentials. Only the authorized destination adapter can rehydrate a required secret just in time, and no secret appears in prompts, model/tool traces, command plans, UI, logs, notifications or exports. | Canary credentials across HTTP, shell, Git, cloud, Kubernetes and database fixtures; token-vault isolation and adapter-only rehydration tests. |
| SAN-AC-04 | Untrusted source instructions remain structurally isolated data and cannot change policy, sanitizer configuration, provider, tools, approvals, recipients or action tiers. | Plain/HTML/script/tool-like JSON, Unicode/bidi/confusable, encoded, image/OCR, transcript, cross-chunk and malicious-MCP injection corpus. |
| SAN-AC-05 | Ingress is scanned before and after bounded canonicalization/decoding; the final joined prompt plus every model/tool-derived text/code/file/image/attachment is rescanned before display, persistence or action. | Nested archive/zip-bomb limits, base64/URL/compressed/split-secret reconstruction, screenshot/OCR and model-regurgitation tests. |
| SAN-AC-06 | Locked-screen, wearable and shared/untrusted-display notifications contain only the approved generic view; logs/telemetry/crash/support paths use allowlisted schemas and contain no free-form sensitive payloads. | Lock/unlock/screen-share/device matrix, Sentry/PostHog/session-replay configuration audit, crash-dump scan and preview-before-share support bundle. |
| SAN-AC-07 | Memory, Vault/Obsidian, embeddings and graph contain only sink-authorized sanitized content or evidence pointers; no raw-then-redact indexing path exists. | Database/vector-neighbor/graph/property/Obsidian scans with seeded secrets, useful-code-context retrieval and cross-zone denial tests. |
| SAN-AC-08 | Every model-provider egress, training/eval job, export or external write is rebuilt from a destination-bound view, rescanned and approved against the final payload hash with recipient/provider, transformations, omissions and residual risk visible. | Slack/email/Jira/Git/support/training fixtures, changed-recipient/payload invalidation and re-identification warning tests. |
| SAN-AC-09 | Source correction/revocation/deletion traverses the Sanitization Lineage DAG and removes or invalidates raw items, views, prompts, caches, memories, Vault notes, vectors, graph data, candidates, replicas and backups while retaining only a non-content tombstone. | Derivative inventory before/after, index rebuild, offline-device/backup expiry, provider-limitation disclosure and tombstone content scan. |
| SAN-AC-10 | Each sanitizer release reports per-class precision/recall/FPR/FNR, residual leakage and calibration plus citation, symbol/line mapping, retrieval, answer, code/test and latency utility loss. Known registered-secret canaries have zero release-suite escapes. | Versioned golden/adversarial corpus, baseline comparison, threshold approval, false-positive override review and rollback. |
| SAN-AC-11 | Gateway failure, stale policy/detector state or unclassified high-risk content blocks new model egress, persistence and external action without blocking unrelated no-context local controls; the UI explains the exact degraded state. | Gateway outage/version-skew/timeout/disk-full tests and zero unsafe-fallback assertion. |
| SAN-AC-12 | A Flask/FastAPI/Vite/Next local preview runs unprivileged in a sanitized environment, binds only to loopback, disables unsafe debug console/reloader behavior, uses clean-browser/no-store and Host/Origin/CORS/CSRF/auth/rate limits as applicable, and cannot access raw quarantine or the privileged executor. | Listener/process/environment scan; `0.0.0.0`/LAN/tunnel, debugger, DNS-rebinding, CSRF/WebSocket, path-traversal and privileged-access denial tests. |
| SAN-AC-13 | Compliance evidence records classification, purpose, authority/consent, provider/region, retention/legal hold, deletion/export and policy version without claiming legal compliance merely from technical controls. | Data inventory/flow map, consent/retention fixtures, cross-border/provider delta, legal-hold/deletion conflict and product-language audit. |
| RESEARCH-AC-01 | Before architecture or design recommendation, the Seed Research Coverage Ledger assigns a stable reference ID to every named tool, product, model, MCP, repository, website, video, Artifact link and relevant source-index URL anywhere in this prompt; canonicalizes duplicates while retaining aliases/occurrences; and gives every item a baseline current investigation or an explicit non-researched status and reason. Deep-reviewing only a shortlist cannot make another seed disappear. | Machine-extracted prompt/source inventory, canonicalization report, complete cited ledger and zero-unaccounted-seed assertion. |
| RESEARCH-AC-02 | For each seed and credible independently discovered reference, record official-versus-third-party identity, URL, prompt location/query, access date/result, version/release/commit where relevant, source/evidence depth, license, security/privacy, maintenance/maturity, claimed-versus-verified capabilities, applicable feature or rejection and `researched / partially researched / inaccessible / superseded / duplicate / not applicable / intentionally deferred`. Every incomplete row names the exact reason, consequence, blocker and minimum official file/access I could provide. | Zero-blank-status ledger, source citations, query/result manifests, inaccessible-item request cards, worker disagreement log and separate baseline-versus-deep-review report. |
| ARTIFACT-DOC-AC-01 | After approved Phase 0 kickoff, maintain a private Artifact Index and use authorized Claude documentation/design/diagram Artifacts wherever they materially improve review of requirements, research, PRDs, journeys, architecture/LLDs/ADRs, threat/data/permission models, evaluations, roadmap, risks, acceptance plans and Gate packets. Every Artifact has a portable versioned companion, citations, verified/inferred/unknown labels, open questions, version/hash, comments/approvals, access level and export path; chat or an external URL alone is not the durable record. | Artifact Index, review/version-diff trail, canonical Markdown/JSON/CSV/Mermaid/Figma or equivalent exports, capability-fallback report and private/synthetic-data access audit. |
| BRAND-AC-01 | Phase 0A evaluates exactly the six supplied classical candidates, explicitly treats Zeno/Seneca/Aristo as Stoics, Cato as Stoic-associated, and Athena/Zeus as mythological; keeps INSHERON and Aurethos retired; proves one coherent family across Assistant, Forge, Counsel, Command, Vault, Link and Glass; and reports source, trademark/collision, namespace/package/app-ID/domain/handle and multilingual wake-word evidence before selection. No candidate becomes a permanent identifier or mark before the Brand sub-gate passes. | Dated ranked evidence table, collision/search receipts, pronunciation/false-wake corpus, retired-name regression test, placeholder-only pre-gate artifact and selected-family manifest. |
| RESEARCH-GITHUB-AC-01 | The Jarvis/Ultron census records every canonical repository URL returned by each dated supported GitHub query/page within documented caps, including repository ID, rank/page, fork/upstream, archived state, incomplete-results/cap status, deduplication key and include/exclude reason; deep-review selection cannot erase an unreviewed result. | Query manifest, raw paginated result files, zero-unaccounted-return assertion, deduplication report, cap/gap disclosure and cited shortlist decisions. |
| REPO-BOOTSTRAP-AC-01 | Only after Brand and Gate 1 approval, authenticate and display the actual GitHub owner before proposing `abheet19/{{family_slug}}`; show exact repository name/visibility/license/README/default branch/protections/secrets/CI settings and create nothing until the T2 repository-create payload is approved. The resulting private repository has a production-quality cited README, security/contribution/licensing notices and verified protections. | Wrong-owner/name-collision/visibility tests, exact create preview and approval receipt, provider-returned repository identity, protection audit and rendered README review. |
| COMMAND-AC-01 | Command is an operational dashboard and persistent streaming chat for all three products, not a decorative overview. Every registered capability is inspectable through one registry-driven typed target/parameter/preview/approval/control surface; only capabilities currently supported and allowed for that identity/device/account are invocable, while denied, disabled, degraded, takeover-only, roadmap and T4 states render truthfully. High-value user-facing workflows additionally receive purpose-built native controls, embedded reusable surfaces or state-preserving deep links. | Automated capability-registry inspectability at 100%, allowed-invocation/denial/degraded coverage, no-wildcard authority report and chat-to-Assistant/Forge/Counsel demos. |
| COMMAND-AC-02 | Command includes Today/Attention, message classifications/Task Candidates, Review Companion/approvals, tasks/agents/live DAGs, projects/sessions/artifacts, Forge, Counsel, global search/launcher, Workstation Control Center, automations, skills/MCP, integrations/OAuth, devices/Voice Lab, models/evals/fine-tuning, Context/Sanitization/Egress Inspector, Vault/privacy/security/audit, health/recovery, backups, costs and settings. | Approved Artifact/LLD plus navigation and empty/loading/degraded/error/stale/outcome-unknown/success state coverage. |
| COMMAND-AC-03 | Dashboard chat supports streaming text/voice, barge-in, attachments and screen/file context, context/egress receipts, model/effort/mode selection, plan editing, approvals, branching/resume/search/export and direct workflow launch. | End-to-end tests with reconnect and cross-product state preservation. |
| COMMAND-AC-04 | Device Fleet shows every paired Mac, phone, browser, CLI and voice satellite with trust, capabilities, current task, app/mic/capture/network/privacy, version, battery/thermal, last seen and handoff/pause/revoke/assistant-data-wipe controls. | Pair, capability negotiation, handoff, stale/disconnect, compromise/revoke and exactly-one-executor tests. |
| COMMAND-AC-05 | Attention Center deduplicates and explains Slack, email, GitHub/GitLab, Jira, Figma, calendar, CI, meeting, device, security and agent events, including source age, “why now,” consequence and action. | Duplicate/out-of-order event tests and approve/deny/edit/snooze/dismiss/receipt flows. |
| COMMAND-AC-06 | Global launcher/search finds authorized apps, commands, people, files, chats, meetings, code, tasks, skills, MCP tools, devices, notes and artifacts without crossing project/privacy boundaries. | Recall/citation, access-denial, staleness, deletion and cross-project-leakage evaluations. |
| COMMAND-AC-07 | All supported customization is accessible through Command: layout, dark-first Theme Studio, glass/motion/sound/accessibility, wake/clap/voice, devices, privacy/memory, models/routing, skills/MCP, integrations, agents, budgets, approvals, automations, notifications, retention and product settings. | Versioned schema, validation, live preview/diff, per-device/product override, profiles, import/export, migration, reset and rollback. |
| COMMAND-AC-08 | A policy/capability simulator explains whether an agent can read or act through a specific device/account/MCP and previews workflows with zero side effects. | Effective-permission explanation matching production policy and dry-run proof of no writes, sends, purchases, deploys or mutations. |
| COMMAND-AC-09 | Workflow Debugger exposes checkpoints, retries, redacted I/O, external effects, failure and recovery, including retry from a safe step and exact restore/compensation limits. | Fault-injected workflow recovered from a checkpoint with irreversible external effects clearly identified. |
| COMMAND-AC-10 | All graphical surfaces use the approved original dark-first 3D Liquid Glass family with Dark default plus System, optional Light, High Contrast, Reduced Transparency and Reduced Motion behavior; content remains legible and responsive. | Token/visual-regression audit, keyboard/screen-reader/200% zoom/contrast tests and representative 60/120 Hz frame-time evidence. |
| REVIEW-COMPANION-AC-01 | The same canonical draft/action version appears in the Mac menu-bar/HUD Approval Capsule, Command Approval Center and paired phone with full target/payload/side-effect data and the Section 5.4.3 **Edit / Regenerate / Explain / Open authoritative source / Approve / Deny / Snooze / Dismiss / Do not draft similar** controls; a single executor and durable outbox prevent duplicate commit attempts. | Interactive Artifact plus focus/IME, version reconciliation, concurrent-device approval, crash/reconnect and keyboard/VoiceOver tests. |
| REVIEW-COMPANION-AC-02 | On an unlocked owner-private Mac, an expandable local notification can show the complete sanitized read-only proposed reply when OS preview settings and policy permit; compact state may show an excerpt. Locked Mac, screen share, meeting/private mode and untrusted display mask sender/project/content. Review opens authenticated full detail, and notification/deep-link open cannot approve or execute. | Full-draft unlocked/private notification plus lock/unlock/external-display/screen-share/APNs payload, generic-preview, deep-link spoof, Reduced Motion and accessibility tests. |
| COMMAND-WORKSTATION-AC-01 | Workstation Control Center reconciles its repositories/worktrees, IDE buffers, terminal panes/sessions, commands/process groups/ports, build/test/debug/QA runs, containers/services/data stores, Git/CI and cloud/Kubernetes targets with runtime truth and the Observable Execution Stream. Start/open/focus/follow/input/interrupt/stop/retry/replay/takeover/cleanup controls preserve action tiers and never expose secrets or kill unrelated work. | Interactive Artifact plus live fixture with concurrent user process, port collision, dirty buffer, failed test, container/service, browser trace, stale cloud context, crash/reconnect and cross-surface reconciliation. |
| ASSIST-AC-01 | On an already OS-authenticated Windows pilot machine—and later independently on the approved Mac—the configured approved wake/address phrase, candidate default **“{{FAMILY}}, attend”** with tested alternatives, plus push-to-talk or locally detected double-clap opens the assistant, runs real health checks, renders the approved skippable post-authentication ceremony, speaks the configured greeting and produces a cited “what is on my plate” briefing. It never replaces or bypasses either OS lock screen, and unknown/offline sources are not reported healthy. | Per-platform cold/warm start, phrase/configuration, locked/unlocked, no-network, failed-source, false-clap, skip, mute and reduced-motion tests with health-event reconciliation and briefing citations; Windows evidence cannot satisfy Mac. |
| ASSIST-AC-02 | System-wide dictation writes approved voice input into the currently authorized application with streaming text, punctuation/formatting commands, correction/undo, vocabulary adaptation, explicit destination focus and visible listening state. Password, payment, recovery-code and other secure fields are denied; mode or target ambiguity stops before typing. | Native and browser text-editor matrix; focus-change/race, secure-field, clipboard-disabled, offline, correction, cancellation and multilingual/accent tests with no unintended keystrokes. |
| ASSIST-AC-03 | Voice can find, open, focus, navigate and click supported Windows pilot apps and websites first—and later independently supported Mac apps—through official app/API targets, DOM/Playwright, platform accessibility/UI Automation and governed vision fallback in that order. Before acting it displays the target and risk; after acting it verifies fresh state. Stale coordinates, prompt injection, changed UI, CAPTCHA, permission/security dialogs or unverified consequential controls stop safely. | Per-platform cross-app/browser benchmark with duplicate labels, moved elements, scrolling, modal, iframe, stale screenshot, injected webpage instruction, CAPTCHA, denial, takeover and postcondition evidence. |
| ASSIST-AC-04 | Activity awareness uses only disclosed, allowlisted sources and shows which app/window/event produced each inference. Sensitive apps, private browsing and user exclusions are masked; pause/private mode takes effect immediately; a screen or message can inform a draft but cannot issue a command. | Source-chip/provenance demo; allowlist/exclusion, screen-lock, secure-field, notification leak, prompt-injection, pause and deletion tests. |
| ASSIST-AC-05 | Slack, email, GitHub/GitLab, Jira, Figma, calendar and CI events are consumed idempotently and may produce cited triage and local drafts, but no send/reply/forward/reaction/read receipt/typing state/comment/update/invite/review or other provider-visible effect occurs without the universal exact preview and approval. Any source, thread, account, recipient/audience, content, attachment, rendering, visibility, schedule or side-effect change invalidates approval. | Duplicate/delayed/out-of-order/edit/delete events; reply/reply-all/thread-broadcast/reaction/read/typing; wrong account/recipient; locked notification; changed draft; injection; deny/edit/approve/revoke; concurrent devices; accepted-response-lost and provider-receipt tests. |
| COMM-CLASSIFY-AC-01 | Every inbound Slack/email/comment event is authenticated and visibly classified as FYI, social, reply requested, Task Candidate, existing-task update, approval request, incident/security, spam/untrusted or ambiguous with evidence/confidence. Classification may triage/draft/correlate but never grants authority or becomes scope. | Adversarial ordinary/question/task/update/incident/spam/ambiguous/edited/duplicate fixtures, user correction, Jira-key/no-Jira, prompt-injection and false-positive/false-negative evaluation. |
| COMM-DRAFT-AC-01 | No provider-visible communication or workflow effect occurs directly from an inbound event or agent output. Every supported outward effect follows local prepare, complete exact preview, explicit bound approval, JIT revalidation, at most one commit attempt under that approval and verified/reconciled authoritative state. | Slack/email/GitHub/GitLab/Jira/Figma/calendar/CI positive, deny, edit, expire, outcome-unknown and provider-audit tests proving zero preapproval writes or blind retries. |
| APPROVAL-BINDING-AC-01 | A single-use short-lived approval binds capability/provider version, acting account/device/executor, destination/recipients/audience/thread, canonical/rendered body, mentions/unfurls/signature, attachments, visibility/schedule, source/context/policy versions, side effects and payload hash. Every material mutation invalidates it. | Field-mutation matrix, distribution-list expansion, account/thread/source edit, attachment replacement, expiry/replay and wrong-device denial. |
| OUTBOX-AC-01 | Every T2/T3 external commit uses a durable action ID/outbox and provider idempotency when available. Timeout/crash/reconnect reconciles before retry; unknown outcome never auto-retries, and edit/delete/retract/compensation is a new approved action. | Provider-accepted-response-lost, timeout before/after commit, duplicate click, concurrent devices, crash, provider-without-idempotency and receipt-reconciliation tests. |
| ASSIST-AC-06 | Calling {{FAMILY}} through the approved WebRTC/SIP endpoint supports a spoken briefing and T0 requests, then hands any T2/T3 action to the explicitly paired trusted device with the canonical task ID. Caller ID, possession, wake phrase and voice match never authorize a consequential action; public-number/SIP cost is approved before provisioning. | Valid/unknown/spoofed caller, lost phone, reconnect, cross-device handoff, expired approval, exactly-one-executor and paired-device biometric tests. |
| ASSIST-AC-07 | Travel, flights, groceries and purchases can be researched, compared and prepared automatically. Commit uses the immutable prepare/commit protocol with exact merchant/account/person/items/substitutions/dates/fees/terms/address/currency preview, current price/inventory revalidation, a single-use hash-bound approval and fresh device biometric. Changed state invalidates approval; idempotency prevents duplicate booking/charge. | Sandbox merchant/booking journeys covering price/availability/substitution/payee change, timeout, retry, double click, network loss, 3-D Secure, CAPTCHA, cancellation/refund and redacted receipt. |
| ASSIST-AC-08 | Every generated report requested by policy lands as versioned Markdown in the authorized Obsidian Vault with stable ID, source citations, backlinks, task/session/product/agent/model/version, repository/branch/commit where relevant, ACL/data zone, freshness and supersession links. Source deletion/revocation cascades to indexes, graph edges, embeddings, caches, candidates and paired devices, or visibly records pending backup/weight limitations. | Markdown/schema/link validation, Obsidian graph traversal, provenance drill-down, conflicting/superseded note, export/import, revocation and deletion-cascade/backup-expiry tests. |
| VAULT-AC-01 | Every sync-eligible note, report and attachment synchronizes E2EE only to its authorized device group while personal/company roots, ACLs, retention and excluded/local-only sources remain isolated. Offline replicas cannot overwrite newer authority and a lost/revoked device cannot decrypt future revisions. | Pair/revoke/key-rotation, offline/reconnect, wrong-zone, lost-device, large-attachment, partial-transfer, relay-inspection and deletion-cascade tests. |
| VAULT-AC-02 | Concurrent Markdown edit, rename, delete, attachment, YAML/frontmatter and backlink/index conflicts preserve both versions and resolve deterministically or through explicit review; silent last-write-wins is impossible. Authorized external Obsidian edits cannot silently become policy, memory, skill, approval or training data. | Two- and three-device conflict corpus, case/Unicode paths, external-editor watcher, crash recovery, tombstone replay, graph/index rebuild and byte-preservation evidence. |
| VAULT-AC-03 | A polished accessible Vault workspace is available in Command and as scoped reusable views in Assistant, Forge and Counsel across desktop, tablet, phone and web, with Markdown editing, fast search, backlinks, provenance/freshness, history/restore, sync/conflict controls and graph/list/table/timeline parity. | Cross-product interactive Artifacts, responsive/native navigation, keyboard/touch/VoiceOver/TalkBack, 200% zoom, large-vault performance, offline and degraded-sync tests. |
| ASSIST-AC-09 | “Open Jira, find ticket X and build it” and the automatic Jira-assignment trigger converge on one Intake. The assembler resolves exact account/project/ticket/repository/branch/HEAD; loads current skills/rules/Graphify; passes first code Context Readiness; retrieves and analyzes relevant NeoSapien MCP evidence or an explicit fallback/waiver; revalidates code when meeting evidence adds entities; and shows the sealed Context Pack and TASK-only diff. It cannot patch TASK or launch/handoff to Forge before approval of their exact hashes. | Ambiguous/stale/duplicate ticket, wrong repository/account, missing/stale skill, code-readiness invalidation, previous-day/DST boundary, meeting-added-symbol, byte-identical outside-TASK, changed-evidence approval invalidation and sender/receiver handoff-hash tests. |
| PERSONAL-UTILITY-AC-01 | Voice/chat can manage exact-device timers, alarms, stopwatch sessions and reminders; perform deterministic calculations/date math/unit/currency/time-zone conversions; retrieve weather/traffic/travel-time with location, source and freshness; and control the exact media app/device. Offline/stale/estimated results are labelled, ambiguity stops or clarifies, and external/subscription/purchase/cast/share/home effects retain their normal tiers. | Offline/local calculation corpus; time-zone/DST, ambiguous-location/device, stale-weather/rate, timer/alarm persistence, media-target, accessibility, account and external-effect tests. |
| MAC-TARGET-AC-01 | A natural command resolves the exact device, signed application/bundle/channel, window/Space, browser and stable user-approved profile alias, tab/URL, provider account/workspace and tool/project/session before action. Ambiguous, missing or changed targets produce a compact clarification or correction rather than opening or editing the wrong surface. | Installed-app registry plus Chrome/Safari/Firefox/profile/account fixtures covering aliases, duplicate app names, multiple channels/profiles, stale windows/tabs, deictic references, correction and negation. |
| MAC-TARGET-AC-02 | The assistant discovers ordinary installed apps and handlers, but automatically opens/focuses only an approved signed handler with a conformance-tested safe app/document/normalized-URL class; custom schemes, macro-capable/quarantined/downloaded executable content, unknown handlers and startup side effects are effect-classified for preview or takeover. Deeper workflows truthfully declare `full semantic control`, `partial`, `open-and-focus only`, `takeover required` or `unsupported`, never bypass protected controls, and verify postconditions. | `NSWorkspace`/Launch Services inventory, signed-handler/file-type/deep-link classification, App Intent/API/DOM/AX/vision ladder, startup-side-effect, unsupported/takeover, protected-surface denial and before/after state tests. |
| JIRA-CREATE-AC-01 | “Open/create a ticket for this task” produces a local Jira draft using the exact approved site/account/project/issue type/fields, resolves ambiguity, previews the complete payload and attachments, obtains the applicable hash-bound approval, creates once through an official API/MCP or stops at a verified visible form before submit, then verifies the returned key/state. | Wrong-site/project/account, required-field, duplicate/idempotency, changed-payload, offline/reauth, UI-fallback, injection, cancel and successful sandbox-creation tests. |
| NATURAL-COMMAND-AC-01 | The selected address name supports natural single and compound commands, follow-up-window ellipsis, deictic grounding, correction, negation, undo and conversational repair—for example “Hey Athena, open Jira in my QuillBot Work profile, draft a ticket, but do not submit”—while the literal Athena token remains conditional on Brand Gate selection. Speech not directed to the assistant, especially during meetings, cannot execute a command. | Indian-English/noisy-room dialogue suite with compound commands, interruption, “not that profile,” “the other one,” target changes, undo limits, human-conversation and meeting false-trigger tests. |
| REALTIME-SLO-AC-01 | The suite meets the approved Interaction Latency Budget for pointer/key acknowledgement, wake/PTT, partial/final ASR, correction, barge/stop, first useful local text/audio, Counsel suggestion, app focus, execution-stream propagation, HUD/cold shell, post-unlock and local search; reports p50/p95/p99 plus cold/warm/offline/degraded/low-power slices and never markets literal zero latency. | End-to-end monotonic traces and error budgets first on the approved Windows pilot baseline, then independently on the approved Apple-Silicon Mac baseline after promotion, under model eviction, concurrency, thermal/battery pressure, network/provider delay and 2D/3D quality modes. |
| PRESENCE-CONTROL-AC-01 | Every primary interface acknowledges immediately with truthful `heard/received/resolving/checking/waiting/blocked/complete` state; I can ask, steer, pause, stop, cancel or take over while work streams. Barge-in/cancel propagates within budget, suppresses duplicate responders and preserves one safe checkpoint without faking or replaying an external effect. | Voice/text/phone/Mac/mobile interruption and handoff tests; stale-target and duplicate-responder injection; action-stream/audit reconciliation; no-fake-completion assertions. |
| MAC-FILES-AC-01 | Finder, Spotlight, Quick Look, file pickers and file operations safely cover search/reveal/preview/open/create/save/rename/copy/move/tag/archive/upload/download/mount/eject/Trash/restore while preventing link/root escape, silent overwrite, wrong-volume transfer and private-file exposure. | Local, cloud-file-provider, external and network-volume matrix with collision, disconnect, quota, quarantine, partial transfer, undo and denial tests. |
| MAC-WEB-AC-01 | Browser automation preserves exact browser/profile/account/window/tab through navigation, forms, drag/drop, file chooser, upload/download, PDF and print preparation; every transfer, redirect/handler and final page state is verified. Password stores, cookies, private history, site-permission grants and CAPTCHA remain inaccessible. | Chrome/Safari/Firefox matrix with duplicate profiles/accounts, punycode/lookalike and redirect-to-app, `file:`, `data:`, `javascript:`, `ssh:` and terminal/custom schemes, universal links, changed default handler, stale tab, downloaded executable/malicious download, wrong upload, popup/modal and CAPTCHA tests. |
| MAC-CONTENT-AC-01 | Text, selection, drag/drop and clipboard actions preserve document version, focus, insertion point, selection and IME; secure fields are denied and clipboard observation is opt-in, visible, scoped, TTL-limited and restorable. Capture/OCR/print/scan targets only the previewed content/device/destination. | Native/web/rich-text editors, multilingual IME, focus race, secret clipboard, screenshot/redaction, print/scan, cancellation and undo/restore tests. |
| MAC-SYSTEM-AC-01 | Common notification, productivity-app, display/audio, Focus, network/VPN, Bluetooth, peripheral, storage, software lifecycle, continuity, lock/sleep/restart/shutdown and settings operations expose truthful state and an explicit effect tier. Native password/biometric/TCC/admin/security confirmation is always completed by the user; unsaved work and active tasks are protected. | Mail/calendar/message drafts, device connect/disconnect, audio/display/printer/scan/VPN/external-drive, install/update/uninstall, dirty-document, restart and protected-panel tests. |
| MAC-AUTOMATION-AC-01 | App Intents, Shortcuts, AppleScript and learned workflows are versioned, inspectable, bounded and policy-equivalent to direct actions; import or demonstration never conveys authority, creates persistence or hides external effects. | Malicious Shortcut/script, schema/version drift, dry-run, login-item/persistence, timeout, duplicate-trigger, rollback, revoke and user-takeover tests. |
| MAC-A11Y-AC-01 | Assistant actions coexist with VoiceOver, Voice Control, Full Keyboard Access, Switch Control, Zoom and multilingual IME; announce state accessibly, preserve/restore focus and selection where possible and provide equivalent nonvisual controls and takeover. | Assistive-technology, window/Space/display, focus restoration, passive-overlay and interrupted-takeover tests. |
| MAC-RECOVERY-AC-01 | Sleep/wake, crash, restart, permission loss, app/UI upgrade, network/storage failure and peripheral removal recover from a safe checkpoint without duplicate effect. Stale approvals invalidate, unsaved work survives and irreversible effects are never presented as undone. | Fault injection across every operation class plus process, file, app, device and event/receipt reconciliation. |
| TERMINAL-AC-01 | Voice/text can open or reuse the exact terminal application/profile/window/tab/pane and supervised session in the resolved local/container/VM/SSH environment, cwd, repository/worktree/branch/HEAD. Opening/focusing never implicitly runs a command or reads unrelated history/scrollback. | Windows Terminal/PowerShell/approved WSL and IDE fixtures first; later Apple Terminal and approved Mac terminal/IDE fixtures after promotion; duplicate-session, wrong-cwd, stale-tab, alias, correction, open-only, local/container/remote and focus tests per platform. |
| TERMINAL-AC-02 | Every command runs from an immutable typed envelope using structured argv by default, explicit target/cwd/preimage, sanitized reviewed startup environment, resource/network/filesystem grants, output limits and timeout. Parsed shell/startup behavior that cannot be classified blocks or moves to a disposable approved sandbox; terminal output/control sequences are untrusted. | Quoting/metacharacter/newline/glob/substitution/redirection/heredoc; malicious `.zshenv/.zshrc/.bashrc`, `BASH_ENV`, `ZDOTDIR`, `PATH/CDPATH`, alias/function/prompt hook, `direnv/.envrc`, IDE bootstrap and setup script; malicious ANSI/OSC; poisoned output, wrong-cwd, environment leak, changed-HEAD and sandbox-denial tests. |
| TERMINAL-AC-03 | Long-running builds, watchers, tests and dev servers retain stable session/process-group ownership and visible owner/lease/expiry; stream bounded redacted logs; expose waiting/input/health/port/resource state; support detach/reconnect; and stop only task-owned processes. A lost session/effect is reconciled before retry, and only actions with declared tested restart/idempotency semantics may restart. | Port collision, healthy-silent, waiting-input, log-flood, child process, lease expiry, lost deploy/migration negative case, sleep, UI/agent crash, reconnect, signal-resistant process, orphan cleanup and unrelated-process survival tests. |
| DEV-SERVER-AC-01 | “Host this project locally” resolves the repository-defined server/toolchain, starts it unprivileged in the exact worktree with sanitized fixtures/environment, loopback-only random port, explicit lease/expiry and clean QA browser, verifies health and URL, streams redacted state, and stops only its process tree. LAN/public/tunnel exposure requires a separate exact approval. | Flask/FastAPI/Vite/Next fixtures; wrong repo/cwd, port collision, missing dependency, debug-console/reloader, `0.0.0.0`, tunnel, secret-env, crash/reconnect, lease expiry and unrelated-process survival tests. |
| TERMINAL-AC-04 | Passwords, raw tokens, passkeys, Touch ID, SSH trust, signing and privileged prompts never enter the model, argv/env/stdin/history or logs and route through the approved secure user/broker flow. `sudo -S`, automatic host-key acceptance and CLI `y` as policy approval are impossible. | Secret-canary scans; admin/password, OAuth/device flow, new/changed SSH fingerprint, hardware-key, signing, cancel/expiry and wrong-account tests. |
| TERMINAL-AC-05 | SSH/SCP/SFTP/rsync/tunnel operations resolve exact allowlisted host/account/environment, paths and direction; disable agent forwarding by default; verify fingerprints and transfers; bind tunnels to loopback with byte/time lease; and cannot follow remote links, reverse direction, enable deletion/exposure or orphan a tunnel silently. | Wrong host/account/path, direction reversal, `--delete`, remote symlink escape, partial/reconnect, changed host key, ambient/forwarded agent denial, non-loopback bind, port collision, byte/time cap and orphan cleanup tests. |
| DESTRUCTIVE-AC-01 | Destructive effects are classified independently of executable/tool name. Broad, unresolved, linked, mounted, protected or production targets fail closed; an allowed scoped action shows canonical targets/count/size/environment, current backup/recoverability and dry-run limits, receives fresh approval and verifies without blind retry. | Shell/Python/Node deletion/truncation, Git clean/reset/history, database drop, cloud/IaC/Kubernetes destroy, package uninstall, process kill, link/empty-variable/wildcard/Unicode/root/home/workspace and permitted disposable-target tests. |
| REPRO-AC-01 | Every terminal/build/test run emits a secret-free Reproducibility Receipt containing executor/OS/toolchain, image/source hashes, argv/script/cwd, environment key names and only non-resolvable task-expiring secret descriptors/version fingerprints, grants, dependency/cache/input state, time/exit/signal, logs/artifacts/processes/ports/nondeterminism and cleanup. It contains no Keychain path, account identifier, bearer handle or replayable secret ID. | Receipt schema, identifier/canary secret scan, expiry test, clean-environment replay, changed lockfile/toolchain/cache/network cases and artifact-hash comparison. |
| WORK-AC-01 | Only a verified configured Jira assignment/actionable-state event automatically creates/deduplicates a read-only Intake; voice/Command/manual Jira-key entry resumes the same Intake. Meeting speech, Slack/email content or a NeoSapien memory may propose or enrich but cannot assert assignment, scope or acceptance criteria, independently start code or mutate TASK. | Signed-webhook/poll receipt, issue/version deduplication, wrong-assignee/status denial, manual/automatic merge, spoofed Jira key, meeting/Slack/email non-start, high-confidence-classifier and quiet/allowlist tests. |
| WORK-AC-02 | For the QuillBot `assistant-prompt.md` workflow, enforce the single ordered state machine: verified/classified event or Task Candidate → exact Jira or policy-permitted Manual Task Intake authority → frozen repository/worktree/branch/HEAD → required skills/rules/knowledge/Graphify load → first repository Context Readiness → task-specific NeoSapien retrieval/meeting analysis → contradiction reconciliation and targeted code revalidation when needed → approved secondary evidence → sanitize/minimize/cite → sealed Context Pack/TASK diff → exact approval → compare-and-swap TASK-only patch → verified complete-prompt Forge acknowledgement → bounded post-handoff validation Explore → LLD/UI Artifact/Plan approvals → Build/QA. A failed, stale or ambiguous upstream receipt blocks downstream mutation and returns to the earliest affected state. | Ordered transition matrix and event trace with input/output hashes, dependency/prohibited-edge assertions, compare-and-swap tests and failure injection at every transition proving unchanged TASK/worktree/Forge state. |
| WORK-AC-03 | The Trigger Registry covers every supported Jira, voice/chat/hotkey/phone, NeoSapien/Counsel, Slack/email/calendar/Figma, GitHub/GitLab/CI/deploy, schedule/wake/unlock, device/network/security/Sentry, connector/OAuth/MCP/model/budget/storage/thermal/update and approved webhook event. Each maps to one bounded effect—observe, notify/brief, enrich/propose Intake, prepare draft, run an approved low-risk workflow or request a named approval tier. Unknown events are notification-only; no incoming event independently authorizes Build, posting, CI rerun, deployment, merge, purchase or another external effect. | Versioned registry/schema, user-approved trigger profile, event-to-effect matrix, dedupe/correlation/quiet-hours/backfill tests and prohibited-transition/invalidation tests across event classes. |
| TASK-CANDIDATE-AC-01 | Every task-like Slack/email/message creates only a read-only Task Candidate with authenticated source, classification, confidence, rationale and uncertainty. It cannot create/edit Jira or TASK, launch/handoff to Forge, edit code or post a reply. | No-key/multi-key/fuzzy/wrong-workspace/edited/deleted/replayed/injected message fixtures and zero-mutation assertions. |
| TASK-CANDIDATE-AC-02 | Exact Jira keys are re-read from the configured Jira account before correlation; fuzzy people/time/project similarity only proposes alternatives and requires confirmation. Source edit/delete, ACL loss, ambiguity or correlation change invalidates affected downstream receipts. | Exact/fuzzy/cross-project/multiple-ticket/repository/meeting, edit/delete/revoke, race and false-merge tests. |
| TASK-CANDIDATE-AC-03 | A candidate without canonical Jira remains held until I link Jira, approve a Jira-create payload or explicitly promote a policy-permitted Manual Task Intake; each path converges on the same context, sanitization, TASK, Forge and LLD/design/plan gates. | Jira-required-policy denial, local draft, approved creation/idempotency, manual exception, clarification-draft, snooze and dismiss tests. |
| WORK-AC-04 | Each Intake publishes the Source Requirement Matrix and documented fallback ladder. Required unavailable/stale/processing/not-authorized sources remain distinct from verified empty/not-applicable and can be satisfied only by the named approved fallback or waiver. | Missing Jira, NeoSapien, `/quillbot-lt-conventions`, triggered skill, knowledge/Graphify, repository, assistant-prompt and Forge-outage cases. |
| WORK-AC-05 | Imperative language such as “build it,” notification/Review open, classifier confidence, standing preference or prior approval authorizes only read-only preparation. TASK mutation requires the exact pack/patch approval; code requires later LLD/design/plan approvals. | Speech/UI/replay/stale-hash/changed-scope denials and proof of no unseen-payload or early-Build authorization. |
| NEO-MCP-AC-01 | Context Bootstrap inspects the authorized MCP registry and user-visible NeoSapien connection instructions specifically for an official NeoSapien/Neo 1 memory server. It records vendor identity/provenance, package/version/endpoint/transport, acting account, tools/resources/schema, scopes, credential destination, region/egress, retention, license and support status. A similarly named or private-backend bridge is never inferred official or installed. | Registry and handshake/capability receipt, official-document evidence or explicit unavailable/unverified state, plus name-collision and unofficial-bridge denial tests. |
| NEO-MCP-AC-02 | Once verified and approved, work intake uses the NeoSapien MCP only through the central gateway with a task-scoped read-only grant. It searches light metadata using Jira/code/people/time anchors, covers previous local calendar day through now then older directly linked material, paginates fully and fetches full transcript/memory content only for selected relevant results. Routine assembly cannot call capture/update/delete/share/bulk-export tools. | Sanitized MCP trace, query provenance, timezone/DST and pagination coverage, account/scope receipt, selected/excluded results, prompt-injection denial and zero-write assertion. |
| NEO-MCP-AC-03 | Successful empty search, still-processing/partial memory, unavailable service, timeout, expired authentication, denied scope and deleted/revoked item are distinct states. If required Neo context is unavailable, TASK mutation stops until an official export/share is supplied or I approve a named per-task waiver; failure is never presented as “no memories.” | No-match, processing, partial-page, timeout, auth-expiry, scope-denial, edited/deleted/revoked, manual-export and waiver tests. |
| NEO-MCP-AC-04 | Meeting analysis distinguishes transcript spans, human notes, generated summaries, proposed/confirmed/superseded decisions, actions, unresolved questions, contradictions and inferences; preserves source IDs/timestamps/speaker confidence/consent/freshness; and reconciles them against Jira, approved design and current code/rules. New code entities reopen the relevant exploration receipt. | Cited analysis with duplicate capture, ambiguous speaker, stale/superseded decision, conflict and new-symbol re-exploration cases. |
| FORGE-HANDOFF-AC-01 | After the approved TASK patch, compute the complete `assistant-prompt.md` hash and deliver the full file or lossless structured equivalent—not only TASK or a summary—to the correct Forge project/chat with Context Pack and Readiness manifests. Forge acknowledges file path/hash, TASK markers, pack version, repository/worktree/branch/HEAD, skill/rule hashes, model/effort/permissions and omissions before planning; truncated/stale/wrong-project input fails closed. | Sender/receiver hash equality, sentinel and marker validation, wrong-chat/repository, truncation, changed-file invalidation, reconnect and duplicate-handoff tests. |
| FORGE-HANDOFF-AC-02 | Direct Forge Ask/Explore is read-only. A QuillBot `assistant-prompt.md` Build validates the complete Section 4/4.1 Intake, sealed sanitized pack, full prompt hash, shared Context Engine state and every required Jira/NeoSapien/Graphify/skills/TASK receipt; Forge cannot recreate, skip or self-waive them. A generic/non-QuillBot Build instead validates its Generic Manual Coding Intake and may omit a company-specific source only when the Source Requirement Matrix records it as genuinely not applicable. | QuillBot and generic direct-chat, truncated/manual-task, wrong project, stale index, missing required source/waiver, false-not-applicable and forged-receipt denial tests. |
| VOICE-AC-01 | The system distinguishes transcription adaptation, meeting-local diarization, my enrolled profile and another person’s consented persistent profile in architecture, storage, UI and policy. It never silently turns an ephemeral speaker into a biometric identity. | Data-flow/policy tests and UI demo for all four states, including unknown speaker. |
| VOICE-AC-02 | My enrollment/adaptation is local-first, inspectable, versioned, benchmarked and deletable; it improves held-out recognition without unacceptable false matches or regression. | Before/after WER and speaker metrics across approved devices/noise/accents, artifact lineage, reset/delete and rollback test. |
| VOICE-AC-03 | Other voices default to ephemeral meeting-local labels. Persistent named recognition is impossible without a valid Speaker Profile Consent Record; withdrawal and deletion change future recognition and cascade through derived artifacts. | Denied-without-consent, enrollment, open-set recognition, withdrawal, deletion-cascade and backup-expiry tests. |
| VOICE-AC-04 | Continuous adaptation is an offline, reviewable candidate workflow; low-quality, uncertain, sensitive, echo/media, replay/deepfake and unconsented segments are rejected, and live sessions never update production weights silently. | Candidate inclusion/exclusion receipts, poisoning/regression/unknown-speaker eval, approval and rollback evidence. |
| VOICE-AC-05 | Voice identity is never sufficient authorization and the system rejects or labels uncertain, replayed, synthetic and unknown voices instead of forcing a match. | FAR/FRR/EER/calibration, unknown-rejection, replay/synthetic/media/crosstalk tests and T2/T3 denial receipts. |
| VOICE-AC-06 | In wake-only mode, untriggered audio remains only in the disclosed RAM ring buffer of at most two seconds by default and produces no transcript, embedding, memory, analytics, training sample, file, database write or network attempt. | Instrumented network/filesystem/memory-boundary test across at least 10,000 non-trigger windows. |
| VOICE-AC-07 | Persistent enrollment cannot occur without an OS-authenticated ceremony and purpose/retention consent. Owner enrollment covers at least two sessions and three supported acoustic conditions; mixed, overlapping, playback, echo, virtual-device and low-quality samples are rejected. | Positive/negative enrollment tests plus sample-quality and provenance receipts. |
| VOICE-AC-08 | On a frozen consented target-hardware set, owner-versus-other matching uses one calibrated threshold and meets an approved release target initially set at FAR ≤0.1% and FRR ≤5% in declared supported conditions, with 95% confidence intervals and device/noise/language slices. Unsupported conditions visibly fall back to Unknown. | Reproducible evaluation report and threshold/version artifact; metrics never market voice as authentication. |
| VOICE-AC-09 | Diarization reports DER/JER, speaker-count/overlap error, p50/p95 stabilization latency, CPU/GPU/RAM, battery and thermals on target Macs/phones and representative public plus private consented data. | Benchmark ADR with approved per-condition thresholds and no unsupported “recognizes everyone” claim. |
| VOICE-AC-10 | Owner adaptation is off by default, versioned and rollbackable. Promotion cannot worsen false-match or spoof-acceptance rate in any supported slice and cannot regress false-non-match rate by more than one absolute percentage point without explicit exception approval. | Base-versus-candidate held-out/impostor/spoof comparison, raw-sample erasure proof and rollback test. |
| VOICE-AC-11 | Local-only mode supports enrollment, diarization, verification, adaptation, export and deletion with network denied; telemetry is off. Mode changes remain visible and a classifier cannot start retained transcription or biometric creation. | Packet capture, offline E2E test, mode-transition denial test and one-action correction/pause evidence. |
| VOICE-AC-12 | Forget/withdraw immediately disables matching; local active artifacts are deleted within one minute, active paired devices within the published target (initially 24 hours), offline devices before next matching use, and backups through documented expiry/cryptographic-erasure policy. No stored voice artifact can be selected for cloning or protected-trait/emotion/employee inference. | Complete artifact inventory, deletion receipts/pending-state UI, offline-device reconnect test and prohibited-secondary-use tests. |
| VIDEO-AC-01 | Every supplied video/Artifact is canonicalized and deduplicated; inaccessible content is marked unverified and triggers an exact official-export/file request rather than an invented description. | Source manifest showing all supplied URLs, duplicate relations, access result and missing-file request. |
| VIDEO-AC-02 | Every received video has a technical manifest, semantic-range index, timestamped audio-evidence status and a machine-readable CSV/JSON Frame Ledger with one record per decoded frame. The ledger records frame index, PTS/time, semantic-range ID, duplicate/held status, scene/motion score, thresholds and classification, keyframe/contact-sheet pointer, automated-versus-semantic inspection status, reviewer confidence/sign-off and decoder/tool version. Corrupt, dropped, unreviewed or semantically unaudited audio/video intervals prevent a “complete audiovisual analysis” claim. | Hash/codec/frame/audio report; retained ledger and generation script/config/version; exactly-once `0–9529` for the 23 August compilation, exactly-once `0–8064` for the 24 August compilation and exactly-once coverage of every other asset’s decoded frame-index set; monotonic-PTS and zero-gap/zero-overlap assertions; duplicate method and variable-timing report; transition/contact-sheet index; timestamped visual observations; independent continuous audio timeline/review or explicit silent/unreviewed boundary. |
| VIDEO-AC-03 | No reference is copied directly and the frame ledger is never used as a reconstruction storyboard. Every adopted motion/material/layout principle maps through evidence to an original prototype; no final direction preserves a reference's frame order, camera path, cut/transition timing, object trajectory, component silhouette, spatial composition, distinctive choreography, CSS, shader, mesh, texture, audio, logo, copy, font, icon, screenshot or interaction code. The result is measured for clarity, accessibility, privacy legibility, responsive behavior, frame pacing and thermal cost. | Motion/Material Evidence Matrix, asset/code/provenance scan, chronological-similarity and side-by-side originality review, A/B/C Artifact comparison and p50/p95/p99 frame/jank evidence. |
| VIDEO-AC-04 | The unlock/system-check ceremony reflects real checks. Pass, degraded, blocked and unknown are distinguishable; a failed or unavailable source can never be animated as healthy merely to complete the sequence. Routine ceremony is skippable and never delays device access. | Injected pass/fail/unknown/offline cases, source drill-down, boot-duration/skip test and audit-event comparison. |
| VIDEO-AC-05 | Every semantic topology edge, node, activity pulse and status has documented meaning and resolves to real provenance, timestamps and action state. Purely decorative particles are explicitly classified as ambient, visually subordinate and prohibited from implying work, health, urgency or success. The same semantic information and operations are available in synchronized table/list/timeline views. | Graph-legend contract tests, fabricated-event and ambient-particle negative tests, graph-to-audit reconciliation, keyboard/VoiceOver traversal and list parity. |
| VIDEO-AC-06 | Optional camera gesture control has calibration, explicit armed/clutch and camera indicators, pause, dead zones, confidence/confirmation, timeouts, tracking-loss/occlusion fail-safe and complete mouse/keyboard/touch/voice parity. It processes landmarks locally and retains no raw frames by default, minimizes bystander capture, never sends camera material to memory/training, and auto-disarms on secure fields, screen sharing, approvals, payments and destructive controls. A gesture, speaker match or wake phrase cannot authorize a consequential action. | False-positive/false-negative and interruption tests across lighting/occlusion/body diversity; raw-frame/network/storage lifecycle inspection; low-confidence/last-command replay denial; secure-surface auto-disarm; indicator test; accidental-purchase/publication/deletion denial; bystander and accessible-fallback evidence. |
| VIDEO-AC-07 | The suite-wide core/face state machine has one tested mapping for asleep, waking, identifying, listening, transcribing, retrieving, planning, acting, waiting, approval, paused, blocked, error, complete, degraded and offline. Text and non-color cues expose the state; no raw chain-of-thought is shown. | State snapshot/motion/earcon matrix, assistive-technology assertions, color-blind test and tool-event-to-visible-state reconciliation. |
| VIDEO-AC-08 | Cinematic 3D is limited to brand, wake/unlock, idle, high-level topology and major transitions. Code, diffs, terminals, transcripts, approvals, payment/security prompts and errors remain calm, opaque enough and readable with 3D disabled. | Representative Command/Forge/Counsel journeys at 3D on/off, 200% zoom, reduced motion/transparency and contrast/visual-regression evidence. |
| VIDEO-AC-09 | Prompt, component, skill, model and visual-template galleries display source, license, version, compatibility, data egress, cost and install/use scope; a reel badge or marketing statement is never treated as license proof. | Provenance/SBOM/license records, blocked-unknown-license test and preview-versus-install approval evidence. |
| VIDEO-AC-10 | For every approved target hardware/OS/browser/resolution/device-pixel-ratio/quality-mode row, define and meet numeric p95/p99 render-frame time, jank percentage, main-thread long-task, input-latency, power and thermal limits while reserving compositor/input headroom beneath the 16.67 ms 60 Hz or optional 8.33 ms 120 Hz display interval. Those intervals are not automatically safe render budgets, and the reel demonstrates neither 120 Hz nor any performance target. Hidden/background rendering suspends and every task remains usable through a semantic 2D fallback. | p50/p95/p99 frame time, jank and long-task distributions, input latency, headroom calculation, GPU/CPU/RAM/battery/thermal-soak and battery-saver traces; minimize/occlusion suspension; WebGL-context-loss and no-WebGL E2E; daylight/SDR/HDR contrast; OLED static-HUD/burn-in mitigation. |
| VIDEO-AC-11 | Command and Forge show a truthful priority-ordered resume state: what is waiting, why, repository/worktree, agent, action/tool trace, pending approval and exact resume/cancel/rollback choices. The trace matches actual events and survives restart/device handoff. | Forced interruption/restart/offline/handoff scenarios, event-log reconciliation, checkpoint restoration and stale-approval invalidation. |
| VIDEO-AC-12 | Desktop/ultrawide, phone and optional secondary screens have documented complementary roles; they do not duplicate decorative HUDs. Device handoff preserves conversation/task/approval state without leaking secrets to the wrong display. | Cross-device role matrix, phone voice/approval journey, lock-screen/privacy masking, revocation and handoff continuity tests. |
| VIDEO-AC-13 | The source video and every derived frame, contact sheet, OCR/transcript, embedding, ledger and annotation remain private, access-controlled, excluded from shipped repositories, demos, marketing and model training, and expire under the approved research-retention policy. | Artifact inventory, ACL/access log, repository/training-corpus negative scan, expiry job and deletion receipts. |
| VIDEO-AC-14 | Desktop-monitor footage is not treated as evidence for mobile/responsive behavior, accessibility, gesture/voice causality, real-time 3D technology, secure unlock or production performance; each claim requires independent current research and an instrumented prototype. | Claim-to-evidence matrix with separate mobile, accessibility, input-causality, security and performance tests. |
| VIDEO-AC-15 | Full-screen flashes, uncontrolled parallax/zoom, focus loss during graph morphs, color-only state, tiny microtype and transparent text over moving content are rejected. Reduced-motion/transparency modes preserve task order, focus, announcements and complete 2D functionality. | Flash-threshold/photosensitivity, vestibular, focus-order, screen-reader/live-region, contrast, 200% zoom and reduced-mode journey tests. |
| VIDEO-AC-16 | Every accessible supplied video produces a timestamped **Feature Observation Ledger** covering every on-screen, demonstrated, captioned and intelligibly narrated capability. Each item is marked `observed`, `claimed`, `inferred`, `unclear` or `not auditable` and mapped to adopt/adapt/reject, destination product, requirement/design state, architecture and acceptance evidence. No feature is copied, silently omitted or treated as proven merely because a reel claims it. | Audio/caption transcript or explicit unaudited boundary plus frame/range links, zero-unaccounted-feature assertion, requirement-ledger diff, safe-disposition entries and implemented/prototyped evidence. |
| DESIGN-EVIDENCE-AC-01 | Gate 2 cannot pass until each uploaded compilation has a machine-readable ledger containing exactly one row per decoded frame, monotonic PTS, duplicate/held classification, cut/motion metrics, semantic-range membership and review status. The 23 August source covers `0–9529`; the 24 August source covers `0–8064`. Every visually unique range and every before/at/after transition set receives semantic review; exact duplicates may share one reviewed representative. Automated decode alone is never called frame-by-frame semantic analysis. | Separate `9,530`- and `8,065`-row CSV/JSON ledgers; per-source zero-gap/overlap/exactly-once assertions, decoder/script/config versions, transition indexes, reviewer sign-off and unresolved-visual-interval count of zero; separate audio status showing the second source is effectively silent. |
| DESIGN-SYNTHESIS-AC-01 | The Reference Transformation and Morphological Matrix covers every supplied clip/range and accepted stronger reference with observed principle → workflow problem → adopt/adapt/reject → transformation → destination product/state → originality risk → accessibility/performance consequence. Every reference contributes a transformed lesson or explicit rejection. | Machine-checked zero-unmapped reference report with timestamped evidence and decisions. |
| DESIGN-SYNTHESIS-AC-02 | A/B/C are deliberate original permutations with identical feature/scenario coverage, not copies, reskins or a collage. Direction A tests the Cinematic Instrument hypothesis; B the Spatial Observatory; C the Obsidian Foundry. The selected system uses a compact semantic core/topology where useful in Command, calm operational panes plus truthful DAG/resume state in Forge, and a restrained low-distraction Counsel overlay while preserving one token/state/motion family. | Same-scenario interactive Artifact comparison, transformation rationale, independent screen-level originality/trade-dress review and owner-selected Artifact IDs/versions/hashes. |
| DESIGN-SYNTHESIS-AC-03 | Before A/B/C are finalized, Sarah creates a private Design Divergence Lab Artifact with at least nine meaningfully different suite-level concept seeds generated by permuting independent geometry, information-architecture, depth, panel, material, typography/density, topology, motion/state and ceremony axes. Every seed records its hypothesis, workflow value, transformation lineage, tradeoffs and advance/reject decision; exactly three substantively different suites advance. The supplied video and this prompt's example direction may inspire questions and abstract principles but cannot predetermine the layout or winner. | Versioned Divergence Lab Artifact, morphological combination matrix, seed-to-final lineage, rejected-concept log, same-scenario coverage check, cosmetic-variant rejection test and independent originality review. |
| DESIGN-WINDOWS-AC-01 | The selected direction is first proven as a coherent Windows pilot experience covering tray/quick panel, command palette, passive/interactive HUD, Command, Forge, Counsel overlay/review, post-authentication ceremony, notification/approval surfaces, Snap/virtual-desktop/fullscreen/multidisplay/mixed-DPI behavior and window restoration, with Windows-native focus/accessibility/security conventions. | Windows interactive Artifact plus native pilot review at approved display sizes; keyboard/touch/Narrator/UIA, high-contrast/reduced-motion, lock/UAC/focus and resource tests. |
| DESIGN-MAC-AC-01 | Gate 2 also proves the selected direction as a coherent **Mac design contract** covering menu bar/quick panel, command palette, transient passive/interactive HUD, full Command window, Forge workspace, passive/interactive Counsel overlay and review window, post-authentication wake ceremony, native notification/permission/Touch ID sheets, multiwindow/fullscreen/Split View/Spaces/Stage Manager, multi-display/mixed-DPI/Retina/safe-area behavior and window restoration. It remains labelled unvalidated until the Windows Pilot Gate and work-Mac approval allow native review. | Interactive Mac Artifact journeys at approved small-laptop, standard desktop and ultrawide dimensions; minimum/latest macOS and Apple-Silicon target matrix; later native-platform review with no inherited Windows pass. |
| DESIGN-MAC-AC-02 | A passive HUD or Counsel overlay never steals typing, selection or IME state from the current app. Explicit activation announces the focus transition; dismissal restores the prior app, window and insertion point where the OS permits. All Mac surfaces pass keyboard, Full Keyboard Access, VoiceOver/Voice Control, 200% scaling, Reduce Motion, Reduce Transparency, Increase Contrast and Differentiate Without Color tests. | Focus/IME/Spaces/fullscreen/multidisplay automation plus accessibility report and unsupported-state disclosures. |
| DESIGN-MATERIAL-AC-01 | GlassMaterialAdapter uses version-gated public current-macOS material APIs where appropriate, supported older-macOS fallback, platform-appropriate Windows treatment and accessible Linux/web/mobile/2D fallbacks. Shared semantics remain coherent without imitating Apple material or forcing custom chrome over native controls. | Platform/OS availability matrix, fallback screenshots/Artifacts, API/provenance review and semantic token conformance tests. |
| DESIGN-XPLAT-AC-01 | Gate 2 includes a Cross-Platform Surface Matrix for macOS, Windows, Linux, web, iPhone/iPad and Android. Every released platform provides a capability-appropriate primary journey for Assistant, Forge and Counsel plus Command/Vault access; individual privileged steps may be `remote-delegated`, `review-only`, `takeover-required`, roadmap or unavailable with a reason, but an entire released core product cannot be reduced to a notification shell. The matrix shows native window/navigation/input/accessibility conventions, material fallback, responsive reflow, delegated/degraded behavior and state-preserving handoff. Before a non-Mac client ships, its complete primary journey passes an interactive Artifact and native-platform review; a roadmap row is never described as supported. | Gate-2 adaptation panels plus per-release interactive Artifact, native-platform review, keyboard/touch/screen-reader tests, product × platform journey evidence and capability-matrix conformance. |
| DESIGN-PERF-AC-01 | Settled tray/menu-bar, hidden/minimized/occluded, idle and static operational surfaces stop continuous rendering. Active 2D/3D meets separate measured frame/input/CPU/GPU/RAM/power/thermal budgets with display/compositor headroom first on the Windows pilot baseline and later independently on the approved Apple-Silicon baseline; all work remains functional in 2D. | Per-platform energy/GPU/frame/jank/long-task traces across idle, streaming, active-2D and cinematic-3D; sleep/wake, low-power, thermal, context-loss and fallback tests with no cross-platform result reuse. |
| DESIGN-GATE-AC-01 | No production code, scaffold, dependency installation, migration or repository creation begins until I approve the exact final interactive Artifact versions for Assistant/Command, Forge and Counsel. Any material change to navigation, component, state, motion, accessibility, platform behavior or reference transformation invalidates the affected approval. | Unchanged-worktree/dependency/infra proof and first implementation change linked to owner-approved Artifact IDs, versions and hashes. |
| ARTIFACT-FEASIBILITY-AC-01 | Claude Code Artifacts are used only for supported static single-page client-side synthetic prototypes within current rendered-size limits; they never claim a backend, viewer authentication, durable form state, arbitrary API/WebSocket access or production integration. Any required unsupported behavior is demonstrated in the explicitly approved local interactive equivalent with the same state/interaction coverage and linked portable source. | Current official capability receipt, Artifact network/storage/auth negative tests, size test, connector-only live-data proof where used, local-fallback parity matrix and owner waiver/approval. |
| BRAIN-AC-01 | The root Orchestrator has one original Strategic Cortex/Second-Brain grammar at menu-bar glyph, HUD and Command scales, driven only by real goal/plan, context, agents, devices, integrations, approvals, risk and execution events. Every semantic pulse/region/link opens a timestamped source/owner/receipt and all meaning/actions have synchronized list/timeline/VoiceOver parity; no private chain-of-thought is shown. | Interactive Artifact and runtime event-to-visual contract tests, fabricated-activity negative test, graph/list reconciliation, keyboard/screen-reader traversal and degraded-source states. |
| CEREMONY-AC-01 | Gate 2 presents original **Abstract Cortex**, name-neutral **Classical Strategist** and **Pure Instrument** wake/post-unlock studies as independently selectable options. Athena-specific classical cues appear only if ATHENA passes Brand Gate and is selected; none copies a statue, actor, character, Marvel/Ultron helmet, weapon or trade dress. The chosen ceremony begins only after legitimate OS authentication, is optional, immediately skippable, cannot steal focus or delay access, and has static/reduced-motion/reduced-transparency/high-contrast/2D/low-power equivalents. | A/B/C interactive studies, clean-room transformation/originality review, name-on/name-off variants, locked/unlocked/failure/skip/focus tests, accessibility variants and measured duration/energy evidence. |
| FORGE-AC-01 | A repository task cannot leave Context state until its receipt identifies repo/worktree/branch/HEAD, index coverage, relevant architecture/symbols/callers/dependencies/owners/history/tests/CI, applicable rules, fully loaded triggered skills and hashes, applicable knowledge-base/Graphify evidence or a justified `not applicable`, freshness, conflicts, omissions and uncertainty. | Receipt plus tests proving stale HEAD/rules/skill/index invalidates it and false-not-applicable knowledge/Graphify is denied. |
| FORGE-AC-02 | Within the valid Workspace Context Scope Record and before any QuillBot-specific memory schema, indexing, retrieval policy, dataset curation, evaluation, adapter/fine-tuning design, repository exploration, LLD, planning, implementation or review, Forge locates and fully loads `/quillbot-lt-conventions` and every triggered `SKILL.md` and required reference, including `/lld-artifact` and design/review skills where available; inaccessible inputs produce a precise user file/export request. | Skill invocation/version/hash log, precedence report, memory/retrieval/training design receipt and missing/stale-skill blocking tests. |
| FORGE-AC-03 | Before Forge is launched for a new QuillBot `assistant-prompt.md` task, the Work Context Assembler searches relevant NeoSapien evidence through the shared governed MCP/adapter path from the start of the previous local calendar day through now, then older ticket/epic/project/participant/code-entity/decision-linked material; it shows timezone, included/excluded/stale/conflicting/processing/unavailable items and citations. Forge validates the sealed receipt and cannot redo or silently bypass it. A generic/non-QuillBot task invokes NeoSapien only when its Source Requirement Matrix marks that source required or optional and authorized. | QuillBot timezone/DST boundary, relevance/deduplication, no-result versus unavailable, processing/partial, pagination, manual-export/waiver and sealed-receipt tests plus generic not-applicable/required-source tests. |
| FORGE-AC-04 | For a QuillBot `assistant-prompt.md` task, exact Jira authority, current code/history/tests/rules/skills, knowledge base/Graphify, selected NeoSapien meeting evidence, and approved Slack/Figma/GitHub/GitLab/email/calendar evidence become a reconciled cited Context Pack before a candidate TASK diff exists. After exact approval, only TASK changes; the complete updated `assistant-prompt.md` and manifests are hash-verified in the correct Forge session before planning. Other coding workflows use their registered required sources and Generic Manual Coding Intake without fabricating company-specific inputs. | QuillBot ordered receipt, byte-identical outside-marker comparison, exact diff, backup/restore, sender/receiver hash equality, wrong-project/truncation denial and citation-quality report plus a generic-project journey with truthful not-applicable sources. |
| FORGE-AC-05 | Every slice follows Explore → LLD Artifact → UI Design Artifact where applicable → Plan Mode → explicit approvals → Build → Review/QA. No code, dependency, migration, repository mutation or external write precedes the applicable approvals. | Unchanged-worktree pre-gate proof plus approved Artifact/node/version/hash linked to first implementation change. |
| FORGE-AC-06 | Forge works through standalone desktop, terminal/TUI, IDE, web/Command and headless/SDK clients with chats/branch/resume, model/effort/harness selection, worktrees, multi-repository projects, multi-agent DAGs, MCP, edit/terminal/test/browser/CI/review/checkpoint flows. | Contract-equivalent task run through at least two clients with the same canonical session/artifacts. |
| FORGE-AC-07 | Ask/Explore/Plan/Build/Review/Debug/QA modes, IDE next-edit completion, Review Workbench, evidence-first Debug, reproducible environments, layered checkpoints and Live Preview Inspector meet their stated behavior and privacy boundaries. | One bug reproduced, diagnosed, minimally fixed, reviewed, browser-tested, CI-tested and restored from each supported checkpoint layer, plus completion latency/leakage eval. |
| FORGE-AC-08 | Completion requires minimal diffs, relevant tests, browser QA where applicable, security/license checks, classified CI results, bounded retries and a human review packet; push/comment/MR/pipeline/deploy/merge retain their action tiers. | Review packet containing plan, diff, tests, screenshots/traces, CI, risks, receipts and known limitations. |
| FORGE-AC-09 | Multi-agent work uses isolated worktrees, leases and typed cited handoffs; agents cannot silently share credentials/context, change scope, approve themselves or keep running after cancellation. | Parallel-worker isolation, stale lease, prompt-injection, cancellation and conflicting-edit tests. |
| FORGE-AC-10 | The Host/Client Capability Matrix truthfully covers the Windows pilot/MVP first, the post-gate macOS reference target, Linux roadmap, desktop, terminal/TUI, IDE, web/Command, headless/SDK and paired remote execution, including where every privileged capability runs and every unavailable/degraded behavior. A canonical session hands off only to a negotiated authorized executor and never implies browser-sandbox or one-platform evidence grants host access elsewhere. | Matrix-to-runtime conformance tests on each released client, unsupported-capability denial, web/phone-to-paired-Windows handoff first, later paired-Mac handoff, offline/reconnect, exactly-one-executor and contract-version compatibility evidence. |
| FINE-TUNE-AC-01 | Command’s Model/Evaluation Lab implements a governed proposal → rights/consent and sanitization review → versioned retrieval/rules baseline → authorized dataset card → local reproducible adapter/fine-tuning experiment → held-out quality, code, memorization/extraction, leakage, latency/thermal/cost comparison → explicit promotion or rejection → canary/rollback path. Personal and organization/repository adapters remain isolated; no raw history, hidden reasoning, secret, third-party speech or unapproved company code trains a model. | Base-vs-retrieval-vs-adapter benchmark, dataset/model cards, source lineage and revocation drill, contamination/canary extraction suite, hardware/energy report, dashboard run controls and rollback to prior model/router policy. |
| AGENT-HANDOFF-AC-01 | Sarah can raise a cited design defect through the internal Agent Coordination Channel; a developer worker can inspect it, request missing evidence, reproduce, propose a minimal fix and return a typed result while both retain isolated worktrees, credentials and task scopes. The handoff is visible in the DAG and never posts to Slack or mutates code until the applicable Intake and build/communication approvals pass. | Synthetic Sarah-to-developer journey with cited design node, accepted/rejected escalation, missing-context request, isolated worktrees, cancellation, conflicting edit, no-external-post assertion and final lineage receipt. |
| SWE-IDE-AC-01 | A supported IDE opens the exact workspace/worktree/file/symbol and preserves dirty/untitled buffers, encoding, line endings, selection and newer disk state while semantic navigation, diagnostics, code actions/refactors, tasks and debugger operations use reviewed adapters. | VS Code-compatible plus one approved native/alternate IDE journey, dirty-buffer/concurrent-edit conflict, semantic-rename preview and debugger-expression risk tests. |
| GIT-SAFETY-AC-01 | Forge records repository/worktree/HEAD/branch/remote/account and complete dirty state before mutation; preserves tracked, staged, untracked, ignored, submodule/LFS and concurrent user work; and uses an isolated worktree by default. Stash/pop, amend, reset, clean, rebase, history rewrite, conflict choice or discard cannot occur as hidden cleanup. | Dirty-tree matrix, concurrent editor, branch/worktree switch, rebase/cherry-pick/revert/bisect/conflict, crash/reconnect, byte preservation, signing and force-push denial. |
| SWE-TEST-AC-01 | Forge derives repository-authoritative runtimes, package manager, lockfile, build and test commands; reviews dependency/scripts/provenance/license/security changes; runs impacted then required broader format/lint/type/build/unit/integration/contract/E2E/accessibility/visual/performance/security checks; and cannot weaken tests, snapshots, coverage or CI merely to pass. | Multi-language fixture with package-manager mismatch, malicious pre/postinstall or setup script, typosquat/provenance failure, global/Homebrew install, unsigned binary, unexpected network/credential access, dependency/lockfile change, deterministic failure, flake, snapshot/coverage regression, skipped-test/test-deletion denial and full evidence packet. |
| SWE-QA-AC-01 | Browser QA produces a replayable clean-profile proof bundle covering supported DOM/CSS/accessibility, console/network/HAR/WebSocket, storage/cache/service-worker, responsive/locale/offline/cross-browser behavior and every declared browser-extension runtime surface. A per-browser/OS matrix marks each feature supported/partial/not-applicable/unsupported; Chrome-specific extension surfaces are never asserted for Safari/Firefox without an equivalent. | Chrome/Safari/Firefox capability matrix and only applicable extension install/update/uninstall, background/service-worker/content-script/popup/options/side-panel-equivalent/toolbar/inline/floating journeys, experiments, redaction and environment hashes. |
| SWE-API-AC-01 | API tooling resolves the exact current schema, method/protocol, endpoint/environment/account and shows the complete redacted request and expected effect; tests auth, TLS, CORS, pagination, rate limits, retry/timeout, idempotency, compatibility and webhook replay across REST/OpenAPI, GraphQL, gRPC and streaming protocols as applicable. | Protocol fixtures, wrong environment/account, changed payload, secret/redaction, duplicate side effect, certificate/proxy and contract-regression tests. |
| SWE-DATA-AC-01 | Database/cache/search/queue/object-store inspection is bounded, least-privilege and redacted. Every migration/backfill/cache purge/queue replay/restore or destructive plan identifies exact environment/resources, estimated rows/size/locks/duration, current backup, expand-contract/rollback limits and verification; production data never enters an unapproved local/model context. | Local/test positive cases and production write, destructive DDL, PII export, stale backup, queue redrive and wrong-environment denials. |
| SWE-INFRA-AC-01 | Every container, VM, cloud, Kubernetes and IaC operation resolves exact host/provider/org/account/project/region/environment/context/namespace and inspects fully resolved configuration, privileges, mounts, networks, secrets and image/config digests. Read/diff/validate/plan precedes mutation; production and destructive effects retain T3. | Rootless container/Compose fixture, malicious include/mount, wrong cloud/kube context, loopback port-forward, Helm/Kustomize/IaC plan, secret scan and production-mutation gate. |
| SWE-CI-AC-01 | Forge inspects the exact pipeline/MR commit, DAG, logs, artifacts and reports; classifies causal failure versus flake/infrastructure/cascade before a bounded retry; and binds approval to provider/account/ref/job/variables/inputs/environment. It never “clicks until green” or exposes protected variables. | Deterministic/flaky/infrastructure/downstream fixtures, changed-ref/variable denial, artifact quarantine and local-equivalent comparison. |
| SWE-OBS-AC-01 | Debugging and incident analysis correlate repository/commit, release/deployment/flag, logs, metrics, traces, profiles, errors and authorized user/session evidence into fact/hypothesis/check/conclusion with source/timezone. Temporary instrumentation is tracked and cleaned; sensitive telemetry never leaks to a model or support bundle. | Fault-injected browser/service/Mac journey, Sentry/OpenTelemetry-style correlation, profile/trace evidence, cleanup and secret/customer-data redaction tests. |
| SWE-SEC-AC-01 | Security review reconciles secret, SAST, DAST, dependency, container, IaC, license, SBOM, signature and provenance findings using affected version, reachability, exploitability, exposure and compensating controls, then verifies the minimal remediation. Active testing outside the approved target is prohibited. | Seeded true/false findings, vulnerable dependency path, prompt-injection boundary, SBOM/provenance and unauthorized-scan/exploitation denial. |
| SWE-RELEASE-AC-01 | A release packet contains exact version/changelog/commits, tests/QA/CI, artifacts/hashes/signatures/notarization, SBOM/provenance, environment/flags, rollout/canary/health checks, owners and tested rollback. Publication, protected signing, merge and production deploy/rollback require the configured T2/T3 approval and current state. | Staging/canary fixture, changed artifact/signature invalidation, failed health check, rollback and post-deploy verification. |
| SWE-ONCALL-AC-01 | An alert starts a deduplicated read-only Incident Intake, resolves exact service/environment/severity/version/deployment, builds a cited timeline and drafts mitigation/communications/postmortem, but cannot acknowledge, assign, remediate or resolve itself. Production restart/scale/flag/rollback/deploy/failover is T3. | Duplicate/stale/wrong-service alert, runbook failure, approval expiry, handoff, rollback and independently verified recovery tests. |
| SWE-E2E-AC-01 | The senior-engineer corpus covers repository onboarding, exploration, LLD/plan, worktree, dependency setup, terminal/dev server, edit/refactor/debug, tests/build, API/data change, browser-extension QA, performance/security review, Git/MR, CI diagnosis, release/deploy preparation, incident follow-up and documentation. Every journey is explicitly shipped/degraded/takeover/roadmap/unsupported with real action tiers. | At least one full synthetic day plus positive, denied, interrupted, crash/reconnect and recovery variants for every shipped family, one canonical task ID and audit reconciliation. |
| COUNSEL-AC-01 | Counsel starts and completes a meeting workflow independently with Command stopped, then reconnects and submits a user-reviewed, provenance-preserving meeting-context proposal to the canonical Intake/Work Context Assembler without lost or duplicated state. Counsel cannot create the sealed Context Pack, patch TASK or hand off to Forge. | Standalone meeting demo, proposal receipt and proof that only the Assembler can produce the later TASK candidate. |
| COUNSEL-AC-02 | Counsel distinguishes direct assistant speech, dictation, human conversation, meeting mode, the user, other speakers, media and assistant echo; ordinary meeting speech never becomes an OS command. | Accent, crosstalk, interruption, media, wrong-speaker, false-trigger and mode-correction evaluations. |
| COUNSEL-AC-03 | Personal Overlay supplies the required compact/expanded controls and supported self-capture exclusion, but never claims undetectability. A preflight shows what others may see and fails closed where platform behavior is unsupported or unverified. | Versioned Zoom/Meet/Teams/Webex/Slack matrix, share/recording preview, multi-display test and unsupported-case warning. |
| COUNSEL-AC-04 | Mandatory preflight verifies sources, levels, transcript, overlay visibility, provider/egress/retention, use case, policy and per-participant consent before capture; joining, withdrawal or scope changes alter behavior immediately. | Positive/denied/withdrawal/late-join/source-change tests and consent-ledger evidence. |
| COUNSEL-AC-05 | Assist-only—retain-nothing leaves no persistent audio, transcript, screenshot, embedding, summary, memory, analytics payload, backup or training example beyond the disclosed minimal audit residue. | Post-session filesystem/database/vector/cache/backup inspection. |
| COUNSEL-AC-06 | Live suggestions prioritize other-speaker questions and provide a short “say this,” key points, depth/tone options, citations, source age, confidence and verified/inference/unknown state without auto-speaking or sending. | p50/p95 latency, WER/diarization, churn, missed-question, wrong-speaker, citation precision, factuality and stale-source evaluation. |
| COUNSEL-AC-07 | Visual context is explicitly selected, previewed, redacted, timestamped, cited and deletable; unrelated screens, notifications, password fields and private chats are not captured by default. | Window/slide/manual-screenshot selection, provider-egress, deletion and leakage tests. |
| COUNSEL-AC-08 | Human notes remain separate and byte-preserved; source transcript is append-only; corrections and AI notes are versioned overlays; decisions/actions distinguish proposed, confirmed, disputed, accepted, superseded and completed states. | Lineage/hashes, crash recovery, correction, note-preservation and lifecycle-transition tests. |
| COUNSEL-AC-09 | Consent-scoped recurring-meeting timelines, Answer Cards, summaries, drafts and meeting-context proposals show provenance, ownership, freshness, ACL and review; any later TASK pack is visibly Assembler-produced after the canonical Jira/code/Graphify/NeoSapien reconciliation. No unreviewed answer becomes memory, policy or training data. | Recurring-meeting demo with conflicts, expiry, owner acceptance, Assembler lineage and rejected-learning test. |
| COUNSEL-AC-10 | Sharing or sending always shows exact recipients/account/domains/content/time range/redactions/permissions/expiry/revoke path and requires T2 approval. Counsel’s MCP remains read-only and cannot capture, mutate consent/transcript, speak, post or approve. | Send-preview/changed-payload denial and positive/negative MCP authorization receipts. |
