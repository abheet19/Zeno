{
  "name": "@abheet19/zeno-browse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zeno Browse — a browser Zeno starts and bounds. A fresh, isolated Chromium window per run, driven one operation at a time over stdio, with no access to the owner's real browser profile, cookies, sessions or extensions.",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": { ".": { "types": "./dist/src/index.d.ts", "default": "./dist/src/index.js" } },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "tsc && node --test --experimental-test-coverage --test-coverage-include=\"dist/src/**\" --test-coverage-exclude=\"dist/src/index.js\" --test-coverage-exclude=\"dist/src/host-node.js\" --test-coverage-lines=95 --test-coverage-functions=95 --test-coverage-branches=90 \"dist/test/*.test.js\"",
    "check": "npm run typecheck && npm run test"
  },
  "devDependencies": { "@types/node": "^20.16.11", "typescript": "^5.6.3" }
}
