{
  "name": "@abheet19/zeno-intake",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zeno Intake — the WorkItem port and its free adapters. A work item I own has arrived is the trigger; Jira was only ever one adapter.",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "tsc && node --test \"dist/test/*.test.js\"",
    "check": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "typescript": "^5.6.3"
  }
}
