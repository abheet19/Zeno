{
  "name": "@abheet19/zeno-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zeno CLI — the demo journeys and the ledger verifier. Lives outside the kernel because it does I/O the kernel's lint forbids.",
  "bin": {
    "zeno": "./dist/src/main.js"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "tsc && node --test \"dist/test/*.test.js\"",
    "check": "npm run typecheck && npm run test"
  },
  "dependencies": {
    "@abheet19/zeno-intake": "*",
    "@abheet19/zeno-kernel": "*"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "typescript": "^5.6.3"
  }
}
