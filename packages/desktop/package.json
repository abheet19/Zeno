{
  "name": "@abheet19/zeno-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Zeno desktop shell — the native Windows application window that hosts the Zeno UI and owns the daemon's lifetime.",
  "main": "main.cjs",
  "scripts": {
    "start": "electron .",
    "typecheck": "node --check main.cjs && node --check preload.cjs && node --check project.cjs && node --check lifecycle.cjs && node --check whisper.cjs && node --check zoom.cjs && node --check meeting-presence.cjs",
    "test": "node --test test/*.test.cjs",
    "check": "npm run typecheck && npm run test"
  }
}
