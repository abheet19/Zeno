{
  "name": "@abheet19/zeno-voice",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zeno Voice — the spoken-command layer. Saying \"Zeno, ...\" becomes a PROPOSAL to the gate, never a direct action; the approval still happens with eyes and a click. Pure wake+grammar over an injected recognizer; the real engine is the browser's Web Speech API.",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "tsc && node --test --experimental-test-coverage --test-coverage-exclude=\"dist/test/**\" --test-coverage-exclude=\"**/public/**\" --test-coverage-lines=95 --test-coverage-branches=85 --test-coverage-functions=95 \"dist/test/*.test.js\"",
    "check": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "typescript": "^5.6.3"
  }
}
